{
  "name": "skins-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Skin & desktop pet system for AI desktop apps (WorkBuddy, TRAE Work, Cursor, Claude Desktop)",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0"
  }
}
